"""
Secret Scanner & Data Leak Detection — Phase 25.

Modules:
  - patterns: 200+ regex patterns for detecting secrets in web responses
  - secret_scanner: Core scanner engine with entropy-based detection
  - git_dumper: Exposed .git repository detection and secret extraction
"""
